class Solution {
    public int sumBase(int n, int k) {
        int num = Integer.valueOf(Integer.toString(n, k));
        int sum = 0;
        while(num != 0) {
            sum += num % 10;
            num /= 10;
        }
        return sum;
    }
}
